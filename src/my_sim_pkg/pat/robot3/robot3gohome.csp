// include main file which has all the definitions and movesets.
#include "robot3main.csp";

// prioritise visited positions when returning home to avoid obstacles or running into new pizzas which can't be carried.
#assert VisitedMoveR3() reaches homeR3;